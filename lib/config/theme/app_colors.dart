import 'package:flutter/material.dart';

class AppColors {
  static const themeColor = Colors.blue;
  static const bgScreen = Colors.lightBlue;
  static const white = Colors.white;
  static const black = Colors.black;
  static const grey = Colors.grey;
  static const blackShade = Color.fromRGBO(78, 78, 78, 1);
  static const floatbuttonBg = Color.fromRGBO(254, 128, 129, 1);
  static const btnColor = Color.fromRGBO(168, 72, 233, 1);
}
